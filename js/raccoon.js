var animIcons = lottie.loadAnimation({
  container: document.getElementById('leaf_bg'),
  renderer: 'svg',
  loop: false,
  autoplay: true,
  path: '../wildthoughts/images/raccoon/data.json' // the path to the animation json
});
